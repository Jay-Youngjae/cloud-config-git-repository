spring:
  datasource:
    url: jdbc:mysql://localhost:3306/msa_step02_order?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

# RestTemplate이 참조하는 설정값
service:
  payment:
    url: http://payment-service/payments

server:
  port: 8080

custom:
  message: "초기 메세지 - PRDD 환경입니다"
  version: "1.0.0"

management:
  endpoints:
    web:
      # 노출할 엔드포인트 목록(기본값은 비공개로 되어있기 때문)
      exposure:
        include: health, refresh